func main()->int
{
    var int y;
    var bool z;
    y <- 2 % 1;
    y <- 12 * 21 + 35 / (41 - 53 + 11);
    z <- true and 1 < 3;
    z <- not z;
    print y;
    print z;
    return y;
}
