func main()->int
{
    var int x;
    var int y;
    var bool z;
    x <- 0;
    x <- x + 1;
    y <- 10 * 21 + 35 / (41 - 53);
    z <- not true and 1 < < 3 and y >= 42 or true = (x < 2); ## syntax error by < <
    print z;
    return y;
}
