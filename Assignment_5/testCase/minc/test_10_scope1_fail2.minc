func main(var int a,var int b)->int
{
    var int la;
    var int la; ## re-define la in the same block is not allowed, which is already defined above
    {
        var bool a;
        var bool b;
        var bool ga;
        ga <- true;
        a <- true;
        b <- true;
    }
    a <- 1;
    b <- 20;
    ga <- 3;
    gb <- 40;
    gc <- true;
    {
        var bool la;
        la <- true;
    }
    return 0;
}
