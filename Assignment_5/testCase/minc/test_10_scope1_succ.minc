
func main()->int
{
    var int a;
    var int b;
    var int ga;
    var int gb;
    var bool gc ;
    {
        var int la;
        var int lb;
        {
            var bool a;
            var bool b;
            var bool ga;
            ga <- true;
            a <- true;
            b <- true;
        }
        a <- 1;
        b <- 20;
        ga <- 3;
        gb <- 40;
        gc <- true;
        {
            var bool la;
            la <- a < ga and b >= gb;
            gc <- gc and la;
        }
        print gc;
    }
    return gb;
}
